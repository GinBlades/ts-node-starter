h1 Show TmpDemo

table
  thead
    tr
      th First Name
      th Last Name
      th Position
      th
  tbody
    tr
      td= tmpDemo.firstName
      td= tmpDemo.lastName
      td= tmpDemo.position
      td
        a(href=`/tmp-demo/${tmpDemo._id}/edit`) Edit
        form(action=`/tmp-demo/${tmpDemo._id}/delete`, method="POST")
          input(type="submit", value="Delete")
